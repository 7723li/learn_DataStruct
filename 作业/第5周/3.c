// Huffman Codes
#include <stdio.h>
#include <stdlib.h>

typedef struct latters Codes;
struct latters
{
	char* data;
	int weight;
};
typedef struct latters* MaxHeap;
MaxHeap CreatHeap(int weight){
	MaxHeap H = (MaxHeap) malloc (sizeof())
}

typedef struct TreeNode* HuffmanTree;
struct TreeNode
{
	int weight;
	HuffmanTree Left, Right;
};

int main(int argc, char const *argv[])
{
	int nums, i;
	scanf("%d", &nums);

	// int stus;
	// scanf("%d", &stus);

	// for(i=0; i<stus; i++){
	// 	for(j=0; j<nums; j++){
			
	// 	}
	// }
	
	return 0;
}
